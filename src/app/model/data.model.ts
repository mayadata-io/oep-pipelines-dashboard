export interface contactDetails {
  name: string;
  email: string;
  company: string;
  tag: string;

}
export interface authAccount {
  marketingUrl: string;
  productUrl: string;
  portalUrl: string;
  accountUrl: string;
}